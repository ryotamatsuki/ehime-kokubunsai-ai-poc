# 国文祭PoC データモデル v2

`data/events.json` は30件すべて架空のイベントです。検索用の `aliases` と
`search_tags` に加え、参加案内・料金構造・アクセス・雨天時対応・バリアフリー・
問い合わせを構造化して保持します。

参加案内の詳細はアプリ側で直接回答し、Modalへ送る候補データには含めません。
`未定` と `null` は補完せず、それぞれ「このPoCデータでは未定です。」と
「この項目は登録されていません。」として案内します。

次イベント推薦の候補は固定IDで持たず、開催日・日ごとの開始終了時刻・市町／地域・
ジャンル／タグから都度計算します。実際の経路や所要時間は計算しません。
